[project]
name = "gmail-phish-scan"
version = "1.0.0"

[deploy]
start_command = "python hermes_phish_detect.py"
restart_policy = "always"

[health_check]
timeout = 300

[phases.setup]
python_version = "3.11"